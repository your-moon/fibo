apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: migrate-claim0
  name: migrate-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
